<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "htttp://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mall.dao.UserMapper">

    <select id="selectById" resultType="com.example.mall.entity.User">
        select id, username, password, salt, type
        from user
        where id = #{id}
    </select>

    <select id="selectByName" resultType="com.example.mall.entity.User">
        select id, username, password, salt, type
        from user
        where username = #{username}
    </select>

    <insert id="insertUser" parameterType="com.example.mall.entity.User" keyProperty="id">
        insert into user (username, password, salt, type)
        values (#{username},#{password},#{salt},#{type})
    </insert>

    <update id="updatePassword">
        update user set password = #{password} where id = #{id}
    </update>

</mapper>